[package]
name = "fast-auto-scrna-kernels"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "kernels"
path = "src/lib.rs"

[dependencies]
ndarray = { version = "0.16", features = ["rayon"] }
rayon = "1.10"
hnsw_rs = "0.3"
libm = "0.2"
